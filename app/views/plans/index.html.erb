<% @plans.each do |p| %>
  <p><%= p.name %> / <%= p.price %></p>
  <% if current_user %>
    <%= link_to "Sign up", new_subscription_path(:plan_id => p.id) %>
  <% else %>
    <%= link_to "Sign Up", new_user_registration_path(:plan_id => p.id) %>
    <% end %>
<% end %>
